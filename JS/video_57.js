let emp = {
    name: "swastik",
    role: "developer",
    salary: 50000
}
 for (const key in emp) {
        const element = emp[key];
        console.log(key,element);
    }
 
for (const c of "swastik") {
    console.log(c)
    
}